# Stability audit status — 2026-05-11

Scope: completion state of `docs/audits/stability-all-2026-05-10.md`.

Methodology: grep `[x]` / `[ ]` against the audit's 3 CRITICALs, 90 WARNINGs,
55 INFOs, 43 Recommendations, and 13 phase headers. Cross-checked against
the last three commits (`7bf88cd`, `198aed8`, `b8ffea0`) plus the uncommitted
Phase 1 W-findings batch in the working tree.

## Headline numbers

| Category                | Done   | Open | Notes                                               |
| ----------------------- | ------ | ---- | --------------------------------------------------- |
| CRITICAL                | 3 / 3  | 0    | All three closed.                                   |
| WARNING                 | 71 /90 | 19   | Concentrated in Phases 9, 11, 10, 12 + 2 leftovers. |
| INFO                    | 6 /55  | 49   | Sweep deliberately deferred.                        |
| Recommendations (R1-43) | 43 /43 | 0    | 5 are `scope: deferred`/`scale-deferred` no-ops.    |
| Phase header checkboxes | 0 /13  | 13   | Never ticked — only sub-items are.                  |

The high-level phase headers at the top of the document (lines 33-45) are
all still `[ ]`. They will read as untouched until every sub-item in the
phase is closed.

## What the user asked about

- **Phase 1 — W-STS-EXIT, W-STS-LEAK still `[ ]`.** Correct. Both were
  _demoted_ from CRITICAL during meta-review and are accurate as still-open
  WARNINGs. W1.1-W1.10 (the original Phase 1 WARNINGs) are all ticked but
  **uncommitted** in the working tree — that batch was about to be committed
  when the prior conversation was compacted.
- **Phase 7 — a few unchecked.** Confirmed: `W-FATALEXIT` (demoted CRITICAL,
  unhandledRejection → fatalExit re-rejection) and `W7.3` (no `sd_notify`).
  Everything else in Phase 7 is closed; `R38` (sd_notify itself) is ticked
  with a `scope: deferred — operator deploys via Docker` annotation.
- **Phase 9 — completely skipped.** Confirmed: only `W9.5` is closed (and
  that one is `scope: scale-deferred`). W9.1, W9.2, W9.3, W9.4, W9.6, W9.7
  are all open. Phase 9 covers `channel-state.ts` / `isupport.ts` /
  `channel-presence-checker.ts` — the [C-ENSURECHAN] critical was closed
  there but the WARNING sweep on the same files wasn't.

## Open items by phase

### Outstanding work

- [ ] **W-STS-EXIT** — `process.exit(2)` in `Bot.connect()` STS gate skips
      teardown (`src/bot.ts:1370-1375`). Throw `STSRefusalError`, catch in
      `Bot.start()`, run `gracefulShutdown` with exit code 2.
- [ ] **W-STS-LEAK** — `messageQueue.flushWithDeadline(100)` can drain
      plaintext after STS upgrade decision (`src/bot.ts:1463-1485`,
      `connection-lifecycle.ts:362-369`). Call `messageQueue.clear()` _before_
      `client.quit()` on STS-upgrade close path; skip the flush on that path.
- [ ] **W-FATALEXIT** — `unhandledRejection` → `fatalExit` re-rejects when
      `bot.shutdown()` throws (`src/index.ts:152-158`,
      `src/process-handlers.ts:35-51`). Inner try/catch in
      `shutdownWithTimeout` to convert reject to resolve and log on a
      dedicated channel.
- [ ] **W7.3** — No `sd_notify` (`src/index.ts:106-122`). R38 deferred
      because operator deploys via Docker; tick W7.3 with the same
      annotation or leave open for a future systemd-target deploy.
- [ ] **W9.1** — RPL_CHANNELMODEIS (324) parses `+l` param without
      `Number.isFinite` guard (`channel-state.ts:577,591`). Reuse the guard
      already present in `processChannelMode`.
- [ ] **W9.2** — Mode-array entries with missing `+`/`-` silently treated as
      remove (`channel-state.ts:411,576-602`). Skip-with-warn for malformed
      direction.
- [ ] **W9.3** — Reconnect mid-353: `clearAllChannels` runs but late lines
      re-create empty records (`channel-state.ts:280-308,498-533`).
- [ ] **W9.4** — NICK collision overwrites without warning
      (`channel-state.ts:377-406`). Netsplit merge loses account/away;
      security-relevant under `$a:` matching.
- [ ] **W9.6** — Drift detection runs only against `configuredChannels`
      (`channel-presence-checker.ts:80-119`). Run-time `.join #help` invisible.
- [ ] **W9.7** — TARGMAX parser silently coerces malformed pairs
      (`isupport.ts:170-186`). `TARGMAX=PRIVMSG:` → `Infinity`; `:0` rejected
      silently.
- [ ] **W10.1** — RSS permanent error treated as transient on add path
      (`plugins/rss/commands.ts:284-313`). Persists feeds on 404 seed; 5
      doomed polls before circuit opens.
- [ ] **W10.5** — RSS per-feed staggering: feeds tick on same 60s boundary
      (`plugins/rss/feed-formatter.ts:109`). Jitter first poll within
      interval.
- [ ] **W11.1** — `rejoin_attempts:<chan>` KV records never deleted
      (`plugins/chanmod/protection.ts:128,154`). KV bloat across reboots.
- [ ] **W11.3** — Stale `pendingRecoverCleanup` / `unbanRequested` fires
      `-im` on rejoin (`mode-enforce-recovery.ts:131-134`,
      `protection.ts:111-113`).
- [ ] **W11.4** — `splitActive` / `splitExpiry` not pruned by 60s time bind
      (`plugins/chanmod/stopnethack.ts:99-106`).
- [ ] **W11.7** — `joinRates` / lockdown counters survive bot ops loss
      mid-window (`plugins/flood/lockdown.ts:48-58`).
- [ ] **W11.8** — `enforcement-executor inFlight` Set has detached promise
      race against teardown (`plugins/flood/enforcement-executor.ts:276-281`).
      Add `disposed` flag at top of teardown.
- [ ] **W12.2** — `help.cooldown_ms` snapshot at init time; live-config
      inconsistent with header/footer (`plugins/help/index.ts:82-87`).
- [ ] **W12.4** — SOCKS5 connect timeout not configured end-to-end
      (`src/utils/socks.ts`). Black-holed proxy → half-open connect.

### INFO sweep (49 items, deferred)

The 49 open INFOs are intentionally low-priority. Spot list of the ones
most worth picking up if doing a sweep:

- [ ] **I1.1** — STS clock-skew vulnerability (`sts.ts`).
- [ ] **I3.1** — No `data/` write-permission check at startup.
- [ ] **I6.2** — `_botIdentifyState='unidentified'` is sticky until next
      disconnect — momentary glitch locks bot for whole session.
- [ ] **I8.1** — `MessageQueue.setRate()` doesn't restart timer (note: R35
      claims this is already fixed at HEAD; if so, tick I8.1 too).
- [ ] **I8.3** — `.repl-command` audit row may persist `.chpass --self <pw>`
      literally up to 256 chars.
- [ ] **I10.5** — ai-chat token-budget JSON corrupt-row reads as zero-spent
      → under-counts budget.

The remaining 43 INFO items are "verified clean", trivial perf nits, or
documentation-only.

## Working-tree state

Uncommitted edits to 41 files — this is the **Phase 1 W-findings batch**
the summary noted as ready-to-commit (W1.1-W1.10, W6.1, W6.2, W2.2-W2.6,
W3.2/3.5/3.7/3.8, etc.). The audit doc has been ticked but the code changes
are unpushed. Once committed, the open count drops from 19 to roughly what
is shown above — the count above already assumes those ticks are accurate.

## Suggested next steps

1. **Commit the Phase 1 sweep checkpoint** — `git status` shows 41 modified
   files with the matching audit-doc tick-throughs. The summary marked
   this as the immediately-next action when the prior session was compacted.
2. **Close Phase 9 (6 WARNINGs)** — single file (`channel-state.ts` /
   `isupport.ts` / `channel-presence-checker.ts`) where the CRITICAL was
   already taken; the WARNINGs are small follow-ons (NaN guards, NICK
   collision warn, drift detection scope). Highest ratio of fixes per
   context window.
3. **Close the 2 demoted-CRITICAL leftovers (W-STS-EXIT, W-FATALEXIT)** —
   shutdown-ordering finishes; both are mechanically simple and touch
   `bot.ts` / `index.ts` only.
4. **Decide W7.3 + W9.3 + W11.x posture** — W7.3 (sd_notify) is paired with
   already-deferred R38; tick or leave with the same annotation. The five
   Phase 11 KV/timer-pruning WARNINGs are repetitive — batch them as one
   commit.
5. **Skip INFO sweep unless an incident motivates one** — 49 items, mostly
   verified-clean or trivial. Better ROI in Phase 9/11 fixes above.

After that, the audit's 13 phase-header checkboxes can finally be ticked
phase-by-phase as their sub-items reach 100% (Phases 2, 3, 4, 5, 6, 8 are
already at 100% if the working-tree batch is committed).
